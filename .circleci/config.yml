version: 2.1
jobs:
  build-and-test:
    docker:
      - image: circleci/node:10.13.0
    steps:
      - checkout
      - run: npm install
      - run: npm test
workflows:
  build-and-test:
    jobs:
      - build-and-test
