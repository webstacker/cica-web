version: 2.1
jobs:
  build-and-test:
    docker:
      - image: circleci/node:10.13.0
    steps:
      - checkout
      - run:
          name: install-npm-wee
          command: npm install
      - run:
          name: test
          command: npm test
workflows:
  build-and-test:
    jobs:
      - build-and-test
